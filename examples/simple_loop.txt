fn main() {
  var i = 0;

  var inner_val : int;

  var result = 'outer: loop {
    i = i + 1;

    inner_val = loop {
      if (i % 2 == 0) {
        continue 'outer;
      };

      if (i > 7) {
        break 'outer i * 10;
      };

      break i + 1;
    };

    if (inner_val > 5) {
      break inner_val * 2;
    };
  };

  result
}
